import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tourshernan.app',
  appName: 'myapptours',
  webDir: 'dist/myapptours',
  bundledWebRuntime: false
};

export default config;
